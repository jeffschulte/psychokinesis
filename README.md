psychokinesis
=============

we will use this to get ... ART!

Setting up build environment
----------------------------
Mac:

`brew install sdl`

Linux:

`sudo apt-get install libsdl1.2-dev`


Compile with `make`
